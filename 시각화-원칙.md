# 오염도 시각화 원칙

> 판정 기준과 상태 정의는 `오염원-정의.md` 참조. 이 문서는 그 결과를 **사람이 보고 판단할 수 있게 그리는 법**만 다룬다.

---

## 1. 목적을 먼저 고정한다

이 그림은 코드 구조를 보여주려는 게 아니다. **"테스트 경계를 어디에 그을 것인가"** 라는 단 하나의 질문에 답하기 위한 도구다.

그림을 보고 다음 셋 중 하나를 고를 수 있어야 성공이다.

- 진입점을 테스트 경계로 삼는다
- 경계를 한 층 내린다
- 여러 서브트리 루트를 각각 경계로 삼는다

이 판단에 기여하지 않는 정보는 전부 뺀다. 콜 그래프 시각화는 노드가 늘어나는 순간 판단 도구가 아니라 감상용 그림이 된다.

### 합격 기준

**아무 설명 없이 5초 봤을 때, 오염원 분포가 "아래에 뭉침 / 전체에 흩어짐 / 진입점 바로 아래" 중 어느 것인지 구분되는가.**

구분이 안 되면 색·배치·밀도 중 하나가 잘못된 것이다. 인터랙션으로 보완할 문제가 아니다. 기본 화면에서 답이 보여야 하고, 인터랙션은 그 답을 **정밀화**하는 용도다.

---

## 2. 시각 채널을 아껴 쓴다

사람이 의식적 노력 없이 읽는 채널은 몇 개 안 된다. 전부 판단에 직결된 정보에만 배정한다.

| 채널 | 배정 |
|---|---|
| 색상(채도) | 노드 상태 (깨끗함 / 오염됨 / 오염원) |
| 수직 위치 | 트리 깊이 |
| 크기 또는 테두리 | 오염도 (역방향 도달 수) |
| 간선 스타일 | 오염 전파 경로인지 여부 |

파일 경로, 함수 길이(LOC), 호출 횟수, 커밋 빈도 같은 건 넣지 않는다. **채널을 하나 더 쓸 때마다 주 신호가 그만큼 묻힌다.** 부가 정보는 전부 툴팁으로 내린다.

---

## 3. 정상 상태가 조용해야 한다

가장 틀리기 쉬운 부분이다.

오염은 리프에서 위로 전파되므로, **상황이 나쁠수록 트리 대부분이 "오염됨" 상태가 된다.** 오염됨을 강한 색으로 칠하면 화면 전체가 빨개져서 아무것도 안 보인다.

| 상태 | 시각 처리 |
|---|---|
| 깨끗함 | 무채색 / 낮은 채도. 눈이 안 가야 한다 |
| 오염됨 | 옅은 색조. 영역으로 인식되되 개별 노드로 튀지 않게 |
| 오염원 | 유일하게 강한 색. 시선을 독점해야 한다 |

핵심 원칙:

> **채도는 "얼마나 나쁜가"가 아니라 "내가 손댈 대상인가"에 비례한다.**

오염됨은 결과일 뿐 작업 대상이 아니다.

한 가지 예외 — 깨끗한 영역은 **색의 부재가 아니라 별도 처리**로 구분하는 게 좋다. 그건 공짜로 얻는 테스트 경계라서 적극적으로 찾아야 하는 대상이지, 배경이 아니다.

---

## 4. 깊이를 수직으로 고정한다

진입점을 맨 위에 두고 깊이를 층으로 내린다. 그러면 판단 기준이 **"강한 색이 얼마나 높이 올라왔는가"** 라는 하나의 물리적 높이로 환원된다.

- 오염원이 아래층에만 → 색이 낮게 깔림 → 진입점까지 회수 가능
- 오염원이 여러 층에 걸침 → 색이 세로로 길게 뻗음 → 경계를 내려야 함

같은 깊이의 노드는 가로로 정렬하되, **부모별로 묶어서 배치**한다. 그래야 서브트리 단위의 뭉침이 보인다.

⚠️ force-directed 계열 레이아웃은 예쁘지만 **깊이 정보가 사라져서 이 판단 기준이 통째로 날아간다.** 쓰지 않는다.

---

## 5. 접기를 기본 동작으로 둔다

깨끗한 서브트리는 이미 결론이 난 영역이다. 펼쳐둘 이유가 없다.

- 기본 상태에서 **깨끗한 서브트리는 접힌 채로** 렌더링하고, 내부 노드 개수만 배지로 표시한다
- 접힌 노드가 곧 **확보된 테스트 경계**다. 접힘 자체가 정보다
- 펼치기는 가능하되 기본값이 아니어야 한다

이 규칙만 있어도 화면에 남는 건 오염 경로 위의 노드들뿐이라, 노드 50개 한계에 잘 안 걸린다.

그래도 클 때:
- 오염원에서 진입점까지의 **경로에 있는 노드만** 남기고 나머지 제거
- 화면을 두 장으로 분리 (전체 구조 / 오염 경로만)

---

## 6. 핵심 인터랙션 — "고쳤다고 치면"

HTML로 만드는 이유의 대부분이 여기 있다.

### 호버

오염원에 마우스를 올리면 **그것 때문에 오염된 조상 전부**가 하이라이트된다.

### 클릭 — 시뮬레이션

그 오염원을 수정했다고 가정하고 전체 상태를 재계산한다.

이게 중요한 이유는 오염도 계산의 함정 때문이다. 어떤 조상이 다른 오염원 때문에도 오염되어 있다면, 이걸 고쳐도 안 깨끗해진다. 단순 도달 수만 보면 이 차이가 안 보이는데, **시뮬레이션은 실제로 깨끗해지는 노드만 색을 바꾸므로 진짜 회수량이 그대로 드러난다.**

요구 사항:

- **누적 선택** 가능해야 한다. "이 세 개를 고치면 진입점까지 도달하는가"가 실제 질문이다
- **전체 되돌리기** 필요
- 시뮬레이션으로 깨끗해진 노드는 **원래 깨끗한 노드와 다른 색.** 현재 상태와 가정 상태가 섞이면 안 된다
- 선택된 오염원 수와 그로 인해 회수되는 노드 수를 어딘가에 숫자로 표시

---

## 7. 그래프 옆에 순위표를 둔다

그래프는 **모양**을 보여주지만 **순서**를 못 보여준다.

오염원을 오염도 내림차순으로 나열한 목록을 옆에 붙인다.

- 목록 항목과 그래프 노드는 **양방향 연동** (한쪽 호버 시 다른 쪽도 반응)
- 목록 표시 항목: 오염도, 진입점 도달 여부, 전역 read/write 구분
- 이 목록이 그대로 작업 순서가 된다

역할 분담:
- **그래프** → 어디를 경계로 삼을지
- **목록** → 무엇부터 고칠지

---

## 8. 재현 시 비교 가능해야 한다

오염원을 하나 고치고 다시 그렸을 때 레이아웃이 통째로 바뀌면 뭐가 달라졌는지 못 읽는다. 자동 레이아웃 도구는 노드 하나만 상태가 바뀌어도 배치를 재배열하는 경우가 많다.

- 자식 노드 정렬 순서를 **이름 등 고정된 기준**으로 못 박는다
- 노드 위치는 **트리 구조에서만** 결정되게 하고, 상태(색)는 위치에 영향을 주지 않게 한다

그러면 수정 전후 화면을 겹쳐 봤을 때 **색만 바뀌어서**, 오염이 위로 걷히는 과정이 그대로 보인다.

---

## 9. 라벨 처리

C 함수명은 길고 접두사가 겹친다. 전체 이름을 다 쓰면 노드가 커져서 배치가 무너진다.

- 공통 접두사는 잘라서 표시
- 노드 안에는 **짧은 이름 + 오염도 숫자**만, 나머지는 툴팁
- 오염원은 라벨을 더 크게 해도 된다. 소수고, 찾아야 할 대상이다

---

## 10. 하지 말아야 할 것

| 안티패턴 | 이유 |
|---|---|
| 모든 호출 간선 그리기 | 공유 노드가 많으면 선이 엉켜 오염 경로 추적이 불가능해진다. 오염 전파와 무관한 간선은 흐리게 하거나 생략 |
| 오염원과 오염됨을 같은 색 계열로 | 수정 대상과 결과가 섞인다 |
| 깊이를 무시한 자유 배치 | 4번의 판단 기준이 통째로 날아간다 |
| 범례 없이 색만 사용 | 상태가 3개 + 시뮬레이션 상태라 범례 없으면 며칠 뒤 본인도 못 읽는다 |
| 애니메이션 전환 | 비교는 정지 화면 두 장으로 하는 게 정확하다 |
| 부가 메트릭 추가 | LOC, 커밋 빈도 등은 주 신호를 묻는다. 툴팁으로 |

---

## 11. 우선순위

하나만 구현한다면 **6번(시뮬레이션)**.

정적인 색칠 그림은 Graphviz로도 충분하다. 하지만 *"이 세 개를 고치면 진입점이 깨끗해지는가"* 에 즉답하는 건 인터랙티브가 아니면 안 되고, 그게 지금 결정하려는 질문 그 자체다.

구현 순서 권장:

1. 깊이 고정 트리 + 3색 (정적)
2. 접기 (깨끗한 서브트리 기본 접힘)
3. 순위표 + 양방향 연동
4. 호버 하이라이트
5. 클릭 시뮬레이션 + 누적 선택
