# 오염 분석: 입력, 판정, 산출

`calltree.json`(사실)을 입력으로 받아 `analysis.json`(판단)을 만드는 단계의 참조 문서.

```
소스코드 ──libclang──> calltree.json ──분석──> analysis.json
                        (사실)                  (판단)
```

---

## 1. 왜 분리된 단계인가

오염 판정 기준은 확정된 것이 아니라 **분석을 진행하면서 바뀐다**. `const`를 제외할지, 함수 내 `static`을 포함할지, 주소만 넘긴 접근을 어떻게 볼지는 실제 코드를 보기 전에는 정할 수 없다.

판정을 추출기에 넣으면 기준을 한 번 바꿀 때마다 전체 재파싱이 필요하다. 분리해두면 `calltree.json`은 소스가 변하지 않는 한 재사용되고, 기준별 `analysis.json`만 여러 벌 만들어 비교할 수 있다.

두 파일은 같은 USR을 키로 쓰므로 조인이 자명하다.

---

## 2. 입력에서 실제로 쓰는 것

`calltree.json` 전체가 필요하지는 않다. 판정에 쓰이는 필드는 다음이 전부다.

| 필드 | 용도 |
|---|---|
| `meta.entry_point` | 도달 가능성 계산의 시작점 |
| `nodes[].calls[].callee` | 콜 엣지 |
| `nodes[].state_uses[]` | 오염원 판정의 유일한 근거 |
| `nodes[].kind` | `declaration`은 내부를 알 수 없는 리프 |
| `nodes[].unresolved_calls` | 비어있지 않으면 서브트리가 불완전 |
| `state[].is_const` | 기준에 따라 제외 |
| `state[].scope` | 기준에 따라 제외 |

나머지(`return_type`, `params`, `loc`)는 분석에 쓰이지 않는다. 리팩토링 실행 시 시그니처를 참조하기 위한 것이므로 `calltree.json`에만 남는다.

---

## 3. 판정 기준 (`criteria`)

결과 파일에 반드시 기록한다. 기준을 바꿔가며 여러 번 돌릴 때 어떤 결과가 어떤 기준에서 나왔는지 구분할 수 없으면 비교 자체가 무의미해진다.

### `exclude_const`

`is_const: true`인 상태 접근을 오염원 근거에서 제외한다.

기본값 `true`. `const` 룩업 테이블이나 상수 설정 구조체는 테스트 가능성을 해치지 않는다. 이를 포함하면 오염도가 부풀어 우선순위 판단이 왜곡된다.

`false`로 두는 경우는 `const` 선언이지만 실제로는 초기화 시점에 캐스팅으로 변경되는 코드를 의심할 때다.

### `include_function_static`

함수 내 `static` 지역변수를 오염원 근거에 포함한다.

기본값 `true`. 스코프만 좁을 뿐 숨은 상태라는 성격이 전역과 동일하고, 오히려 외부에서 리셋할 방법이 없어 테스트 관점에서는 더 나쁘다.

`false`로 두면 "전역만 먼저 정리한다"는 축소된 범위의 결과를 얻는다.

### `addr_as`

`access: "addr"`(주소만 취한 접근)을 무엇으로 간주할지.

| 값 | 의미 |
|---|---|
| `read` | 낙관적. 오염원 수가 최소로 나온다 |
| `write` | — |
| `readwrite` | 보수적. 권장 기본값 |
| `manual` | 오염원으로 잡되 수동 확인 대상으로 표시 |

`&g_state`를 넘긴 지점에서는 읽기인지 쓰기인지 정적으로 판정할 수 없다. `read`로 뭉뚱그리면 실제 변경을 놓치고, 그 결과 "깨끗하다"고 판단한 서브트리가 실제로는 오염되어 있어 테스트가 깨진다. 놓치는 쪽이 과잉 계상보다 비싸므로 `readwrite`가 기본이다.

---

## 4. 산출 필드

### `is_impure` / `impurity_reasons` — 오염원

**자기 자신이 직접** 숨은 상태에 접근하는 노드. 실제 수정 대상이다.

```
is_impure = state_uses 중 criteria를 통과한 항목이 하나라도 있는가
impurity_reasons = 그 항목들의 target USR 목록 (중복 제거)
```

후손이 오염되어 있는 것은 여기에 영향을 주지 않는다. 이 구분이 무너지면 트리 위쪽 노드가 전부 수정 대상으로 잡혀 과잉 설계로 이어진다.

### `is_contaminated` — 오염됨

자신은 오염원이 아니지만 **후손 중에 오염원이 있는** 노드. 부수적 피해이며, 오염원이 해소되면 함께 풀린다.

```
is_contaminated = (is_impure == false) AND (후손 중 is_impure인 노드가 존재)
```

**규약**: `is_impure`인 노드는 `is_contaminated`를 `false`로 둔다. 자기 자신이 원인인 노드를 부수 피해로 세면 두 집합의 구분이 흐려진다. 스키마는 이를 강제하지 않으므로 분석기에서 지킨다.

### `contamination_degree` — 오염도

이 오염원 하나를 고쳤을 때 **몇 개의 노드가 풀리는가**.

```
contamination_degree = 자신 + 역방향으로 도달 가능한 조상의 수
오염원이 아닌 노드는 0
```

수정 대상들 사이의 우선순위를 정하는 유일한 객관적 근거다. 오염도 12인 소스 하나가 오염도 1인 소스 다섯 개보다 먼저다.

같은 콜리를 여러 번 호출해도 조상은 한 번만 센다(집합 크기).

### `is_clean_subtree_root` — 깨끗한 서브트리 루트

서브트리 전체에 오염원이 없어, **지금 당장 비용 0으로 테스트를 붙일 수 있는** 경계.

```
is_clean_subtree_root =
      서브트리에 오염원이 없다
  AND 부모 중 하나 이상이 오염되어 있다 (또는 자신이 진입점)
  AND unresolved_calls가 서브트리 전체에서 비어 있다
```

**두 번째 조건이 핵심이다.** 이것을 빼면 모든 리프 노드가 깨끗한 서브트리 루트로 잡힌다. 리프 하나하나에 테스트를 붙이는 것은 의미가 없고, 우리가 원하는 것은 "깨끗한 영역의 가장 위쪽"이다. 부모가 오염되어 있다는 것은 거기서 경계가 끝난다는 뜻이다.

**세 번째 조건**은 함수 포인터 호출이 서브트리 안에 있으면 그 아래를 알 수 없기 때문이다. 깨끗해 보이는 것이지 깨끗한 것이 아니다.

### `scc_id`

`null`로 둔다.

재귀·상호재귀 때문에 그래프에 순환이 있지만, 역방향 도달성을 visited 집합으로 계산하면 순환은 자동으로 처리된다. SCC 축약은 위상순서 DP로 최적화할 때 필요한 것인데, 단일 진입점 콜트리의 노드 수는 많아야 수백 개라 순진한 방식으로도 즉시 끝난다.

필드는 스키마에 남겨둔다. 나중에 규모가 커져 최적화가 필요해질 때 형식을 바꾸지 않기 위해서다.

---

## 5. 처리 순서

1. **가지치기** — `entry_point`에서 BFS로 도달 가능한 노드만 남긴다. `calltree.json`에는 TU 전체가 들어 있다.
2. **오염원 판정** — 각 노드의 `state_uses`를 `criteria`로 필터링. 노드 로컬 판단이며 그래프를 보지 않는다.
3. **오염도 계산** — 엣지를 뒤집고, 각 오염원에서 역방향 BFS. 방문 집합의 크기가 오염도.
4. **오염됨 / 깨끗한 서브트리 루트** — 정방향으로 각 노드의 서브트리에 오염원이 있는지 확인. 부모 상태와 대조해 경계를 확정.

2번이 그래프와 무관하다는 점이 중요하다. 기준을 바꿔 다시 돌릴 때 재파싱이 필요 없는 이유가 여기에 있다.

---

## 6. 경계 사례

**`kind: "declaration"` 노드**
정의를 보지 못했으므로 내부 상태 접근을 알 수 없다. 기본적으로 깨끗한 리프로 취급한다(낙관적). 이 가정은 대부분 맞지만, 내부 상태를 가진 라이브러리 함수(`strtok`, `rand`, `localtime` 등)에서 깨진다. 진입점 트리에 걸린 `declaration` 노드 목록은 따로 뽑아 눈으로 확인한다.

기준으로 승격하려면 `criteria`에 필드가 추가되므로 `schema_version`을 올려야 한다.

**진입점 자신이 오염원인 경우**
오염도는 1(자기 자신)이지만, 진입점 바로 아래에 오염원이 몰려 있는 분포는 전략 선택을 크게 바꾼다. 숫자보다 분포를 본다.

**오염원이 없는 경우**
모든 노드가 `is_contaminated: false`, 진입점이 유일한 `is_clean_subtree_root`가 된다. 정상적인 결과이며, 진입점에 바로 테스트를 붙이면 된다.

---

## 7. 결과를 읽는 순서

1. `is_impure: true`인 노드를 `contamination_degree` 내림차순으로 정렬 → **수정 대상 우선순위**
2. `is_clean_subtree_root: true`인 노드 → **지금 당장 붙일 수 있는 테스트 경계**
3. 오염원들의 위치 분포(트리 하단 밀집 / 산재 / 진입점 인접) → **전략 선택**

1번과 2번은 독립적이다. 2번은 아무것도 고치지 않고 오늘 할 수 있는 일이고, 1번은 그 경계를 위로 밀어올리기 위해 해야 할 일이다.
